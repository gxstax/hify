/**
 * Provider module API (model providers & model configs).
 * Fill in typed functions here, e.g.:
 *   export const listProviders = (page: number, pageSize: number) =>
 *     get<PageResult<Provider>>('/providers', { params: { page, pageSize } })
 */
export {}
