package com.hify.provider.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.hify.provider.entity.ModelConfig;

/** Mapper for {@link ModelConfig}; JSON extra_params handling lives on the entity. */
public interface ModelConfigMapper extends BaseMapper<ModelConfig> {
}
